name: CI for Medibook

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x] # ← actualizado para cumplir requisito de Vite

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm' # acelera instalaciones

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      # Opcional: tests si los configuras
      # - name: Run tests
      #   run: npm test
